description: Batch lot/code successfully deactivated
content:
  application/json:
    schema:
      $ref: ../../schemas/responses/RevocationResponse.yaml
    example:
      success: true
      lotId: string
      status: ACTIVATED
