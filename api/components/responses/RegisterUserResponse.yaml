description: User registered successfully
content:
  application/json:
    schema:
      $ref: ../../schemas/responses/RegisterUserResponse.yaml
    example:
      success: true
      username: admin
