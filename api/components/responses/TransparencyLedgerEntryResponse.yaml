description: Cryptographic block appended to the log chain
content:
  application/json:
    schema:
      $ref: ../../schemas/responses/TransparencyLedgerEntryResponse.yaml
    example:
      success: true
      entry: null
