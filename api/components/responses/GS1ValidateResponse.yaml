description: GS1 format validation result returned
content:
  application/json:
    schema:
      $ref: ../../schemas/responses/GS1ValidateResponse.yaml
    example:
      success: true
      valid: true
