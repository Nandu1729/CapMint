description: Investigation action successfully complete
content:
  application/json:
    schema:
      $ref: ../../schemas/responses/InvestigationActionResponse.yaml
    example:
      success: true
      investigationId: string
      status: ACTIVATED
