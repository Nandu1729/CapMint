description: Invalid or missing Authorization Bearer token
content:
  application/json:
    schema:
      $ref: '../../schemas/common/ErrorResponse.yaml'
    example:
      success: false
      error:
        statusCode: 401
        code: "UNAUTHORIZED"
        message: "Invalid or missing Authorization token."
