# Module Dependencies

The module graph should remain a directed acyclic graph. Core modules should depend on shared primitives, while downstream modules should consume upstream services through stable contracts.
