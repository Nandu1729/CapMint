# Service Boundaries

CapMint should be split into clear bounded contexts such as Identity, Budget, Minting, Verification, Evidence, and Transparency. Each context owns its own state and exposes explicit contracts to others.
